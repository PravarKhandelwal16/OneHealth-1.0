name: one_health_app
description: "Intelligent Unified Medical Record System"
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # I've set these to the most compatible versions for you
  lucide_icons: ^0.257.0
  file_picker: ^6.1.1
  dio: ^5.4.0
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true